import React, { Component } from 'react';
import Button from '../components/Button';

export class ButtonsView extends Component {
   render() {
      return (
         <Button btnText="button primary" />
      );
   }
}

export default ButtonsView;
